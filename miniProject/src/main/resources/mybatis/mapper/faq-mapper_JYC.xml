<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.green.miniProject.dao.IFAQDAO_JYC">

	<select id="getFAQListTargetMem" resultType="com.green.miniProject.domain.FAQ">
		SELECT * FROM faq WHERE (faqtitle LIKE CONCAT('%', #{keyword}, '%') OR faqcontent LIKE CONCAT('%', #{keyword}, '%')) AND faqtarget = mid;
	</select>  
	<select id="getFAQListTargetCom" resultType="com.green.miniProject.domain.FAQ">
		SELECT * FROM faq WHERE (faqtitle LIKE CONCAT('%', #{keyword}, '%') OR faqcontent LIKE CONCAT('%', #{keyword}, '%')) AND faqtarget = cno;
	</select> 	
  	
</mapper>  